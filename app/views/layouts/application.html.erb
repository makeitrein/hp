<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Hpfic" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Hpfic" %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%# Modernizr is required for Zurb Foundation %>
  <%= javascript_include_tag 'vendor/modernizr' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <link href='http://fonts.googleapis.com/css?family=Oswald' rel='stylesheet' type='text/css'>  <%= csrf_meta_tags %>
</head>
<body>

<div class="row header">
  <div class="small-12">
    <h1>HPFic</h1>
  </div>
</div>

<div class="row">
  <div class="small-12">
    <%= render 'layouts/navigation' %>
  </div>
</div>


<main role="main">
  <%= render 'layouts/messages' %>
  <%= yield %>
</main>
</body>
</html>
